# Commands and Functions

Demonstrates registering custom Yarn commands and functions using the `YarnBindingLoader` system. Binds game actions (camera shake, screen fade, inventory, health) to Yarn commands so they can be called from dialogue scripts.
